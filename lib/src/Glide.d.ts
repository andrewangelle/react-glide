import { PropsWithChildren } from 'react';
import { GlideProps } from './types';
import './reactGlide.css';
export declare function Glide({ autoPlay, autoPlaySpeed, infinite, dots, height, width, onSlideChange, children }: PropsWithChildren<GlideProps>): JSX.Element;
